def computepay():
    if(hrs>40):
        
        p=((40*rate)+(hrs-40)*(1.5*rate))
        return p
    else:
        
        p=rate*hrs
        return p

hrs = float(input("Enter Hours:"))
rate= float(input("Enter rate:"))
p = computepay()
print(p)
